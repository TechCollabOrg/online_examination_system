---
description: Keep README.md updated with user-facing changes
alwaysApply: true
---

# README 同步规则（必须遵守）

当你对项目做任何会影响“用户怎么使用”的修改时，必须同步更新对应的 README，让它们永远是最新说明书。

## 三份 README 的分工

| 文件 | 何时更新 |
|------|----------|
| 根目录 `README.md` | 全站功能说明、角色能力、跨前后端流程、安全与部署总览 |
| `online-exam-system-backend/README.md` | 后端启动、配置、SQL、API 索引、后端排错（见 `readme-sync-backend.mdc`） |
| `online-exam-system-frontend/README.md` | 前端启动、路由、代理、Electron 打包、前端排错（见 `readme-sync-frontend.mdc`） |

修改某一子模块时，**至少**更新该子模块 README；若同时影响全站使用方式，一并更新根 README。

## 必须更新的典型情况

- 修改了启动方式、端口、开发服务器配置（例如 `vue.config.js`、代理、host、open、环境变量）
- 增加/删除/改名了重要功能入口或页面路径、REST 接口路径
- 增加/修改了部署方式（打包、Nginx、Docker、静态资源路径等）
- 增加/修改了对外访问方式（局域网访问、公网访问、反向代理、HTTPS 等）
- 新增或变更数据库脚本（`sql/`）

## 更新原则

- 用“初中生也能照做”的语言写清楚：要运行什么命令、打开什么地址、失败时先检查什么
- 不写太长；但要可复制执行（命令、URL、端口要明确）
- 子模块 README 文末维护「最后更新」日期（`YYYY-MM-DD`）
