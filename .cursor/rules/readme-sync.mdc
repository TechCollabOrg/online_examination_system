---
description: Keep README.md updated with user-facing changes
alwaysApply: true
---

# README 同步规则（必须遵守）

当你对项目做任何会影响“用户怎么使用”的修改时，必须同步更新根目录的 `README.md`，让它永远是最新说明书。

以下情况 **必须** 更新 README（至少补充一段「怎么做 / 怎么验证 / 常见问题」）：
- 修改了启动方式、端口、开发服务器配置（例如 `vue.config.js`、代理、host、open、环境变量）
- 增加/删除/改名了重要功能入口或页面路径
- 增加/修改了部署方式（打包、Nginx、Docker、静态资源路径等）
- 增加/修改了对外访问方式（局域网访问、公网访问、反向代理、HTTPS 等）

更新原则：
- 用“初中生也能照做”的语言写清楚：要运行什么命令、打开什么地址、失败时先检查什么
- 不写太长；但要可复制执行（命令、URL、端口要明确）
