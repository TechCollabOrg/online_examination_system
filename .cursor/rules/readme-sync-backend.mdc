---
description: 修改后端时同步更新 online-exam-system-backend/README.md
globs: online-exam-system-backend/**
alwaysApply: false
---

# 后端 README 同步规则（必须遵守）

当你修改 `online-exam-system-backend/` 下任何会影响**使用方式**的代码或配置时，必须同步更新同目录的 **`README.md`**。

## 必须更新 README 的情况

- 修改启动方式、端口、`application*.yml`、`env.example`
- 新增/删除/改名 REST 接口或 Controller 路径前缀
- 新增/变更 `sql/` 脚本或数据库初始化步骤
- 修改环境变量名、默认值、存储（local/minio）、JWT/AES 配置说明
- 调整 Maven 构建命令或依赖版本要求

## 更新原则

- 用可复制命令和明确 URL/端口
- 与根目录 `README.md` 不重复堆砌业务说明；业务细节放根 README，本 README 侧重**如何启动、配置、排错、目录与 API 索引**
- 文末「最后更新」日期改为当天（`YYYY-MM-DD`）

## 同时涉及前后端时

若改动也影响前端使用方式，一并更新根 `README.md` 与 `online-exam-system-frontend/README.md`（见 `readme-sync-frontend.mdc`）。
